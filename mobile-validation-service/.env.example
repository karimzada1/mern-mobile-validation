PORT=3001
# Get 1,000 FREE credits — no credit card needed — at https://veriphone.io
# Sign up → go to dashboard → copy your API key
# Without this key the service still validates numbers and returns country info,
# but operatorName will show line type (Mobile, Fixed Line, etc.) instead of real carrier name.
VERIPHONE_API_KEY=your_veriphone_api_key_here
